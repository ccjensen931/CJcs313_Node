<!-- <div class="mt-5 d-flex">
        <div class="ml-5 p-3">
            <a href="composeMessage.php"><button type="button" class="btn btn-primary">Compose</button></a>
            <ul class="list-group">
                <% for(let i = 0; i < messages.length; i++) { %>    foreach ($resultSet as $row)
                    <li class="list-group-item">
                        <a href="homeInbox.php?id="<%= messages[i].message_id">
                            <p style="display:inline;"><%= messages[i].sender</p>
                            <% if(messages[i].read) %>
                                <p style="color:red;display:inline;margin-left:15px">NEW</p><p style="display:inline;margin-left:85px"><%= messages[i].subject %></p></a></li>
                            <% else %>
                                <p style="display:inline;margin-left:135px"><%= messages[i].subject %></p></a></li>
            </ul>
        </div>
        <div class="mt-3 ml-5">
            <div class="card">
                <div class="card-body">
                    <p>' . $result['message_text'] . '</p>
                    <form action="homeInbox.php" method="post">
                        <input type="hidden" name="DeleteMessage" id="' . $_GET["id"] . '" value="' . $_GET["id"] . '" placeholder="">
                        <button type="submit" class="btn btn-danger" style="display:inline;">Delete</button>
                    </form>
                </div>
            </div>
        </div>
    </div>-->